version: '2'
services:
  geniowebpp-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/GenioWebpp/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=GenioWebpp
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
